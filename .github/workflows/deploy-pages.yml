name: Deploy site to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Write Supabase config from secrets (optional, for local TTT demo)
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_PUBLISHABLE_KEY: ${{ secrets.SUPABASE_PUBLISHABLE_KEY }}
        run: |
          if [ -n "$SUPABASE_URL" ] && [ -n "$SUPABASE_PUBLISHABLE_KEY" ]; then
            python3 -c "import os; u=os.environ['SUPABASE_URL']; k=os.environ['SUPABASE_PUBLISHABLE_KEY']; open('website/supabase-config.js','w',encoding='utf-8').write('export const supabaseUrl = '+repr(u)+';\nexport const supabasePublishableKey = '+repr(k)+';\n')"
            echo "Wrote website/supabase-config.js (not published to Pages; prototype-only deploy)."
          else
            echo "No SUPABASE_URL or SUPABASE_PUBLISHABLE_KEY secrets: skipping config."
          fi
      - name: Configure Pages
        uses: actions/configure-pages@v6
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: website/prototype
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
